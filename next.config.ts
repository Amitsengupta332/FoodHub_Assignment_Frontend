import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */

  images: {
    // domains: ["res.cloudinary.com"],
    domains: ["i.ibb.co", "img.com", "res.cloudinary.com"],
  },
};

export default nextConfig;
